# IDEA: Post-Market Merge Protocol (Deferred Deployment)
**Status**: 🌿 Sprouting
**Priority**: P1

## 1. 개요 (Abstract)
장 중(Market Hours)에는 시스템의 안정성이 최우선이므로, 긴급한 Hotfix가 아닌 이상 **모든 코드 변경사항을 별도 브랜치에 격리(Isolation)하고, 장 마감(15:30) 이후에 일괄 병합(Batch Merge) 및 배포**하는 프로토콜을 수립한다.

## 2. 문제 정의 (Problem)
- **Risk**: 장 중 배포 시 컨테이너 재시작으로 인한 데이터 수집 중단 (Tick Gap 발생).
- **Conflict**: 핫픽스 적용은 필요하지만, 운영 중인 프로세스를 건드리고 싶지 않음.

## 3. 제안 프로세스 (Solution)
### Phase 1: Isolation (장 중)
1. `feature/` 또는 `hotfix/` 브랜치 생성.
2. 모든 변경사항(`docs`, `scripts`, `tests`)을 해당 브랜치에 커밋 & 푸시.
3. 운영 서버에서는 해당 브랜치를 `git fetch`만 해두고 **Checkout 하지 않음**.
4. 필요 시(예: 검증 스크립트 실행), 임시 디렉토리나 별도 컨테이너에서 해당 브랜치 코드를 사용.

### Phase 2: Batch Merge (장 마감 후)
1. **Trigger**: 15:40 KST (장 마감 10분 후).
2. **Action**:
    - `git checkout develop`
    - `git merge hotfix/XXX`
    - `docker-compose up -d --build` (전체 재배포)
3. **Validation**: Smoke Test 및 Integration Test 수행.

## 4. 기대 효과
- **Zero Downtime**: 장 중 수집 프로세스 절대 보호.
- **안정성**: 충분한 변경사항이 모인 후 검증된 상태로 배포.
- **심리적 안정**: 운영자가 장 중에 시스템을 건드리는 부담 제거.

## 5. 금일 적용 시나리오
- 현재(`develop`) -> `hotfix/recovery-prevention` 브랜치 생성.
- `docs`, `tests`, `scripts` 모두 이 브랜치에 커밋.
- 15:40까지 대기 후 Merge.
